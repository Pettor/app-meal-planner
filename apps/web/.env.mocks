# Targets the Mocks Server (tools/mock) started by `pnpm dev:mocks`.
# Keep the port in step with `server.port` in tools/mock/mocks.config.js.
VITE_CONNECT_HOST=http://127.0.0.1
VITE_CONNECT_PORT=3200
