# React version
VITE_APP_VERSION=$npm_package_version

# Vite Proxy options
# Targets the dotnet backend (apps/backend) HTTP endpoint when run via `docker compose up`.
VITE_CONNECT_HOST=http://localhost
VITE_CONNECT_PORT=5060
